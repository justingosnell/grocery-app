{
  "name": "grocery-app-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for the grocery app.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/migrate.js",
    "check": "node --check src/server.js && node --check src/migrate.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
